# Empty = same-origin /api (Vite dev server proxies to the API).
# Production (Vercel): set to your API origin — required or /api requests hit Vercel and 404.
# VITE_API_BASE_URL=https://ledgerguard.onrender.com
VITE_API_BASE_URL=